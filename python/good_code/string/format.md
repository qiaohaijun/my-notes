```python
    model_name = "{}_lr{}_bz{}_filter{}".format(FLAGS.flag,
                                            FLAGS.learning_rate,
                                            FLAGS.batch_size,
                                            FLAGS.filter_size)

```

这段代码的优点
1. 清晰
2. 简洁
